name: firstProject
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: npm install
        run: |
          make install
        env:
            CI: true
      - name: Run Linter
        run: |
          make lint