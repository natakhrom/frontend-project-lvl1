name: firstProject
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository code
        uses: actions/checkout@v2
      - name: npm install
        run: |
          make install
      env:
          CI: true
      - name: Run Linter
        run: |
          make lint